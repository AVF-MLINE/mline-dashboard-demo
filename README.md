# React дашборд для МИС (АГ + СД)

Демо-экран с фокусом на:

- нозологии АГ/СД;
- 3 режима отображения (`Сводный`, `Клинический`, `Операционный`);
- роли (`Врач`, `Управленец`);
- кнопку `Подготовить summary / сводку данных` с генерацией краткого AI-резюме по текущему контексту.

## Адреса версий

- `/` — страница сравнения версий;
- `/v1` — исходная версия дашборда;
- `/v2` — улучшенная версия с clinical/management блоками;
- `/v3` — оптимизированная версия с усиленной визуальной иерархией.
- `/v4` — версия на основе реального отчёта эффективности АГ.
- `/v5` — презентационная версия с тем же набором блоков, что в референсном макете.

## Запуск

```bash
npm install
npm run dev
```

## Логотип

Если нужно использовать ваш оригинальный логотип, положите файл в:

`/Users/andreyf/Documents/TEST_CODEX/public/logo.png`

Компонент автоматически подхватит его. Если файла нет, отображается SVG-резерв.

## Сборка

```bash
npm run build
```
